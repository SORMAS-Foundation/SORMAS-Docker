version: '3.8'

services:
  etcd:
    build: etcd
    image: ${DOCKER_REGISTRY}/hzibraunschweig/central-etcd:${CENTRAL_DOCKER_VERSION}

  backup:
    build: backup
    image: ${DOCKER_REGISTRY}/hzibraunschweig/central-backup:${CENTRAL_DOCKER_VERSION}
