version: '3'
services:
  sormas:
    build:
      context: sormas/.
      args:
        SORMAS_URL: ${SORMAS_URL}
    image: docker.netzlink.com:5000/sormas/sormas:latest
    environment:
      - SORMAS_POSTGRES_USER=${SORMAS_POSTGRES_USER}
      - SORMAS_POSTGRES_PASSWORD=${SORMAS_POSTGRES_PASSWORD}
      - PGPASSWORD=${SORMAS_POSTGRES_PASSWORD}
      - MAIL_FROM=${MAIL_FROM}
    ports:
      - "6080:6080"
  postgres:
    build: postgres/.
    command: -c 'config_file=/etc/postgresql/postgresql.conf'
    image: docker.netzlink.com:5000/sormas/postgres:latest
    environment:
      - POSTGRES_PASSWORD=${SORMAS_POSTGRES_PASSWORD}
      - POSTGRES_USER=${SORMAS_POSTGRES_USER}
    volumes:
      - psqldata:/var/lib/postgresql/data
  apache2:
    build:
      context: apache2/.
    links:
      - sormas
    depends_on:
      - sormas
    image: docker.netzlink.com:5000/sormas/apache2:latest
    environment:
      - SORMAS_SERVER_URL=${SORMAS_SERVER_URL}
    volumes:
      - ./apache2/certs/:/usr/local/apache2/certs/
    ports:
      - "80:80"
      - "443:443"

volumes:
  psqldata:
